#ifndef _GATES_OF_SHELL_H
#define _GATES_OF_SHELL_H

/* Libraries imported to whole project */
#include <stdio.h>
#include <stddef.h>
#include <unistd.h>
#include <stdlib.h>

char *get_line(void);
char **check_validity(char *);

#endif
